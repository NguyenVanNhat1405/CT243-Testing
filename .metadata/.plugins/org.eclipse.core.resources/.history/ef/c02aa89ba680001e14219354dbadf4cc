package unit_testing;

import org.testng.Assert;
import org.testng.annotations.AfterMethod;
import org.testng.annotations.BeforeMethod;
import org.testng.annotations.DataProvider;
import org.testng.annotations.Test;

import thuvien.tinhtoan.PhepTinh;
public class TestPhepToan {
	@BeforeMethod
	public void hienthitruoc() {
		System.out.println("Before testcase");
	}
	@AfterMethod
	public void hienthisau() {
		System.out.println("After testcase");
	}
	
	private static final double delta = 1e-15;
	
	@Test(dataProvider="testdata")
	public void TestCong(float x, float y, float expected) {
		float actual = PhepTinh.Op('+', x, y);
		Assert.assertEquals(actual, expected, delta, "tinh toan sai");
		System.out.println("Tinh toan dung");
	}
	@Test(dataProvider="testdata1")
	public void TestTru(float x, float y, float expected) {
		float actual = PhepTinh.Op('-', x, y);
		Assert.assertEquals(actual, expected, delta, "tinh toan sai");
		System.out.println("Tinh toan dung");
	}
	@Test(dataProvider="testdat2")
	public void TestNhan(float x, float y, float expected) {
		float actual = PhepTinh.Op('*', x, y);
		Assert.assertEquals(actual, expected, delta, "tinh toan sai");
		System.out.println("Tinh toan dung");
	}
	@DataProvider(name="testdata")
	public Object[][] TestDataFeed()
	{
     ReadExcelFile config = new     
          ReadExcelFile("D:\\Kiem Thu Tu Dong\\Bai tap them ve Data driven testing\\Baitap1\\Thu vien va Du lieu kiem thu\\Tesstdata.xlsx");
		
				
		int rows = config.getRowCount(0);
		
		Object[][] data = new Object[rows][3];
		
		for(int i=0; i<rows; i++)
		{
			data[i][0] =(float)config.getNumericData(0, i, 0);
			data[i][1] =(float)config.getNumericData(0, i, 1);	
			data[i][2] =(float)config.getNumericData(0, i, 2);
		}
		
		return data;
	}
	@DataProvider(name="testdata1")
	public Object[][] TestDataFeed1()
	{
     ReadExcelFile config = new     
          ReadExcelFile("D:\\Kiem Thu Tu Dong\\Bai tap them ve Data driven testing\\Baitap1\\Thu vien va Du lieu kiem thu\\Tesstdata.xlsx");
		
				
		int rows = config.getRowCount(1);
		
		Object[][] data = new Object[rows][3];
		
		for(int i=0; i<rows; i++)
		{
			data[i][0] =(float)config.getNumericData(1, i, 0);
			data[i][1] =(float)config.getNumericData(1, i, 1);	
			data[i][2] =(float)config.getNumericData(1, i, 2);
		}
		
		return data;
	}
	@DataProvider(name="testdata2")
	public Object[][] TestDataFeed2()
	{
     ReadExcelFile config = new     
          ReadExcelFile("D:\\Kiem Thu Tu Dong\\Bai tap them ve Data driven testing\\Baitap1\\Thu vien va Du lieu kiem thu\\Tesstdata.xlsx");
		
				
		int rows = config.getRowCount(2);
		
		Object[][] data = new Object[rows][3];
		
		for(int i=0; i<rows; i++)
		{
			data[i][0] =(float)config.getNumericData(2, i, 0);
			data[i][1] =(float)config.getNumericData(2, i, 1);	
			data[i][2] =(float)config.getNumericData(2, i, 2);
		}
		
		return data;
	}
}

